on:
  push:
    branches:
      - refs/tags/v*
        - 
name: Create Release and publish to nuget

env:
  VERSION: 0.0.2-pre

jobs:
  build:
    name: Create Release
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
                                                                         
